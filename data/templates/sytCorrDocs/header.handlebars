<style>
    .tbl{ width: 100%; }
    .cel{ width: 33.33%; }
    .cel2{ width: 25%; }
    .dertxt{ text-align: right; }
    .centxt{ text-align: center; }
    .bld{ font-weight: bold; }
    .tipo{ font-family: Arial; }
    .pad{ padding-top: 0.2em; padding-bottom: 0.2em; }
</style>
<table class="tbl tipo">
    <tr>
        <td class="cel pad">&nbsp;</td>
        <td class="cel centxt bld pad">{{correlativos.banco.empresa}}</td>
        <td class="cel dertxt bld pad">{{fecha}}</td>
    </tr>
    <tr>
        <td class="centxt bld pad" colspan="3">Correlatividad de documentos</td>
    </tr>
</table>
<div class="tbl centxt tipo">
    <div class="pad">
        <span class="bld">Del&nbsp;{{correlativos.banco.del}}&nbsp;al&nbsp;{{correlativos.banco.al}}</span>
    </div>
    <div class="pad">
        <span class="bld">Banco:</span>&nbsp;{{correlativos.banco.nombre}} ({{correlativos.banco.simbolo}})&nbsp;&nbsp;&nbsp;
        <span class="bld">No. de cuenta:</span>&nbsp;{{correlativos.banco.nocuenta}}
    </div>    
</div>