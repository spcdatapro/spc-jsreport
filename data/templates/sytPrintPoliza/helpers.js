function printCod(estot, codigo) {
    return +estot === 0 ? codigo : '';
}

function esTotalStr(estot) {
    return +estot === 0 ? 'izqtxt' : 'dertxt bld padder';
}

function esTotal(estot) {
    return +estot === 0 ? '' : 'total bld';
}

function printVal(monto) {
    return +monto !== 0 ? monto : '';
}